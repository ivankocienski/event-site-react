// import { useAppDispatch } from '../data/hooks';
// import { loadSummary } from '../data/features/summarySlice';

function Root() {
  // const dispatch = useAppDispatch();
  // const summary = await dispatch(loadSummary());

  return (
    <div>
      <h1>A Root thing</h1>
      
    </div>
  );
}

export default Root;

// <p>Found {summary.partnerCount} partners and {summary.eventCount} events.</p>